{
  "source": "User-supplied NBA 2K HQ APK native ATTRIBUTES_GetCapBreakerBoostValuesForAttrAtIndex + extracted 21-attribute weighting tables",
  "status": "native arithmetic and native weight selection recovered; HQ screenshot regression now matches all supplied sequences",
  "exactRecoveredMath": {
    "stepFactor": "15 + trunc((350 - 14 * currentRating) / 74)",
    "boost": "max(1, round(((maxWeight - selectedWeight) / maxWeight) * stepFactor))",
    "clamp": "boost = min(boost, naturalMax - currentRating)",
    "repeat": "sequentially five times"
  },
  "weighting": {
    "playerType": "best player type selected once from the starting build state, then held fixed for all five breaker steps",
    "selectedWeight": "raw HeightBasedAttributeWeight for the target attribute (no AttributeRatingWeightScale multiplier)",
    "maxWeight": "maximum raw HeightBasedAttributeWeight among the mapped builder attributes for that same height/player type",
    "important": "Cap Breaker math uses the raw height/player-type table; rating-scaled weights remain part of the OVR calculation only."
  },
  "validationCaveat": "Validated against the user-supplied 2K HQ Cap Breaker screenshots for the 6'5\" SG regression build: 20/20 visible attribute sequences match."
}